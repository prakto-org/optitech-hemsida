---
title: 'OptiTech CLI command: status'
subtitle: "Show the branch's live OptiTech state"
summary: >-
  The OptiTech CLI `optitech status` command shows the branch's live OptiTech state. It is
  an alias of `optitech config status`, so you can check what is currently deployed
  without going through the config command group.
enableTableOfContents: true
---

The `status` command shows the branch's live OptiTech state. It is an alias of [`optitech config status`](/docs/cli/config#status), so it reports the same information without the `config` prefix.

<CliUsage command="status" />

<CliOptions command="status" />

```bash
optitech status
```
