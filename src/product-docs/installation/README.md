# Installation

OptiTech is delivered as a set of containers — frontend, backend, database, and reverse proxy — that can run locally via Docker, on a VPS, or on Kubernetes via the Helm chart. The diagram below sketches how the pieces fit together.

!System architecture

> This section is being expanded.

Material to come: prerequisites, local Docker setup, virtualisation and remote deployment, deploying on a VPS, the Helm chart, S3 storage, mailer, custom certificates, secrets management, and instance upgrades.
